//Establish Server creation and connection
import app from "./app.js";
const PORT = 3000;
app.listen(PORT, () => {
    console.log(`Welcome to Blood Dapp backend server at PORT ${PORT}`);

})







